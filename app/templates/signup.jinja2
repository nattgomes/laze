{% extends 'index.jinja2' %} {% block content %}

<div class="col-sm-12">
  <form class="formulario_signup" action="/newUser" method="post" enctype="multipart/form-data">
    <div class="container">
      <p class="notification"> ATENÇÃO: Este formulário destina-se ao cadastro de novo usuário. Para atribuição de perfil para o mesmo, acesse o menu "Gerenciar perfis".</p> </br>
      <div class="form-row">
        <div class="form-group col-md-6">
          <label for="name">Nome</label>
          <input type="text" name="name" class="form-control" placeholder="Ex.: João" value="{{ name }}">
          <small class="error" class="form-text text-muted">{{name_error}}</small>
        </div>
        <div class="form-group col-md-6" >
          <label for="name">Sobrenome</label>
          <input type="text" name="lastname" class="form-control" placeholder="Ex.: da Silva" value="{{ lastname }}">
          <small class="error" class="form-text text-muted">{{lastname_error}}</small>
        </div>
      </div>
      <div class="form-row">
        <div class="form-group col-md-6">
          <label for="name">CPF</label>
          <input type="text" name="cpf" class="form-control" placeholder="Digite sem pontos ou travessão" value="{{ cpf }}">
          <small class="error" class="form-text text-muted">{{cpf_error}}</small>
        </div>
        <div class="form-group col-md-6">
          <label for="name">Telefone</label>
          <input type="text" name="phone" class="form-control" placeholder="Ex.: 999123456" value="{{ phone }}">
          <small class="error" class="form-text text-muted">{{phone_error}}</small>
        </div>
      </div>
      <div class="form-row">
        <div class="form-group col-md-6">
          <label for="formGroupInput" class="name_input">Usuário</label>
          <input type="text" name="username" class="form-control" placeholder="Digite o nome de usuário" value="{{ username }}" />
          <small class="error" class="form-text text-muted">{{username_error}}</small>
        </div>
        <div class="form-group col-md-6">
          <label for="formGroupInput">Email</label>
          <input type="text" name="email" class="form-control" placeholder="Digite seu e-mail" value="{{ email }}"/>
          <small class="error" class="form-text text-muted">{{email_error}}</small>
        </div>
      </div>
      <div class="form-row">
        <div class="form-group col-md-6">
          <label for="formGroupInput">Senha</label>
          <input type="password" name="password" class="form-control" placeholder="Digite sua senha" />
          <small class="error" class="form-text text-muted">{{password_error}}</small>
        </div>
        <div class="form-group col-md-6">
          <label for="formGroupInput">Confirmação de senha</label>
          <input type="password" name="verify" class="form-control" placeholder="Redigite sua senha" />
          <small class="error" class="form-text text-muted">{{verify_error}}</small>
        </div>
      </div>
      <span id="signup_span">
        <button id="signup_btn" class="btn_laze" type="submit">Criar cadastro</button></br>
      </span>

    </div>
  </form>
</div>

{% endblock %}
